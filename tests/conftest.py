import pytest

"""
import tempfile
from stimascraper.scraper import StimaScraper

pdf_path = tempfile.mkdtemp()

# Define a scraper fixture
@pytest.fixture(scope="module")
def test_scraper():
    scraper = StimaScraper(pdf_path=pdf_path)
    yield scraper
"""
